{% raw -%}
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: app
    ports:
      - "${DB_PORT:-54325}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
{% endraw %}
